# Media Library Manager Environment Variables
# Copy this file to .env and modify as needed

# Server configuration
HOST=0.0.0.0
PORT=9988
STATE_FILE=data/app-state.json

# If running with docker, the above are handled by compose.yaml defaults
